"""
Module vs calsses
module is simple file that contains python code.
Basically a python module is simply a file that contains code.This code can be executed directly or it can be important
when we use the word module.When we import a module the code becomes available to us through the modules name.

"""